#include<stdio.h>
int main() {
   float cir,  cir_a;

printf(" enter the radius of circle: ");
scanf("%f", &cir);

 
cir_a = 2 *3.14* cir;

printf(" area of circumference : %f\n", cir_a);

return 0;
}