#include<stdio.h>
int main() {
  int leng, brea, tri_a;

printf(" enter the length and breadth of the rectangle: ");
scanf("%d", &leng, &brea);

 tri_a = leng*brea;


printf(" area of triangle : %d\n", tri_a);

return 0;
}